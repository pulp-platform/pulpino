set(CONV_SOURCES convolution.c conv_kernels.c)
if (${GCC_ETH})
    if (${USE_RISCY})
        add_application(convolution "${CONV_SOURCES}" FLAGS "-DPROFILE -DDOT_P" LABELS "sequential_tests")
    else()
        add_application(convolution "${CONV_SOURCES}" FLAGS "-DPROFILE" LABELS "sequential_tests")
    endif()
else()
    add_application(convolution "${CONV_SOURCES}" FLAGS "-DPROFILE" LABELS "sequential_tests")
endif()