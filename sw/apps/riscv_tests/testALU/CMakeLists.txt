if (${GCC_ETH})
    if (${GCC_ETH_SLIM})
      add_application(testALU testALU.c FLAGS "-DGCC_ETH_SLIM" LABELS "riscv_tests")
    else()
      add_application(testALU testALU.c FLAGS "-DGCC_ETH" LABELS "riscv_tests")
    endif()
else()
  add_application(testALU testALU.c LABELS "riscv_tests")
endif()
