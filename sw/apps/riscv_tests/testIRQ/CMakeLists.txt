if (${GCC_ETH})
    if (${USE_RISCY})
        add_application(testIRQ main.c FLAGS "-DUSE_RISCY -DGCC_ETH" LABELS "riscv_tests")
    endif()
else()
    add_application(testIRQ main.c LABELS "riscv_tests")
endif()
