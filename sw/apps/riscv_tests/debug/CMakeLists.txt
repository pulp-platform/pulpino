if (${GCC_ETH})
 if (${USE_RISCY})
  add_application(debug main.c TB run_debug.tcl FLAGS "-DGCC_ETH -DUSE_RISCY" LABELS "riscv_tests")
 else()
  add_application(debug main.c TB run_debug.tcl LABELS "riscv_tests")
 endif()
else()
  add_application(debug main.c TB run_debug.tcl LABELS "riscv_tests")
endif()
