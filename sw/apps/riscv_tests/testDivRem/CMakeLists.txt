if (${GCC_ETH})
    if (${USE_RISCY})
        add_application(testDivRem testDivRem.c LABELS "riscv_tests")
    endif()
else()
    add_application(testDivRem testDivRem.c LABELS "riscv_tests")
endif()
